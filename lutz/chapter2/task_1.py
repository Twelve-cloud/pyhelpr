#! /usr/bin/env python3
# -*- coding: utf-8 -*-

# >>> 2 ** 16
# >>> 2 / 5, 2 / 5.0
# >>> "spam" + "eggs"
# >>> S = "ham"
# >>> S * 5
# >>> S[:0]
# >>> "green %s and %s" % ("eggs", S)
# >>> 'green {0} amd {1}'.format('eggs', S)
# >>> ('x',)[0]
# >>> ('x', 'y')[1]
# >>> L = [1, 2, 3] + [4, 5, 6]
# >>> L, L[:], L[:0], L[-2], L[-2:]
# >>> ([1, 2, 3] + [4, 5, 6])[2:4]
# >>> [L[2], L[3]]
# >>> L.reverse(); L
# >>> L.sort(); L
# >>> L.index(4)
# >>> {'a': 1, 'b': 2}['b']
# >>> D = {'x': 1, 'y': 2, 'z': 3}
# >>> D['w'] = 0
# >>> D['x'] + D['w']
# >>> D[(1, 2, 3)] = 4
# >>> list(D.keys()), list(D.values()), (1, 2, 3) in D
# >>> [[]], ["", [], (), {}, None]
